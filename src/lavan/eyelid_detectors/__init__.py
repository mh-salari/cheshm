"""Eyelid detectors.

No built-in implementations yet. The category exists so the GUI's
detector discovery is uniform across all four targets (pupil, glint,
limbus, eyelid); drop a subpackage with ``detect_eyelid`` here to
register one.
"""

__all__: list[str] = []
